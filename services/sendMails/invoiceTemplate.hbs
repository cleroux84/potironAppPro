<!DOCTYPE html>
<html lang="fr">
<head>
<meta charset="UTF-8">
<title>Facture {{invoiceNumber}}</title>
<style>
    body { font-family: Arial, sans-serif; margin: 40px; }
    h1 { text-align: center; }
    table { width: 100%; border-collapse: collapse; margin-top: 20px; }
    th, td { border: 1px solid #333; padding: 8px; text-align: left; }
    th { background-color: #f2f2f2; }
    .totals { margin-top: 20px; text-align: right; }
</style>
</head>
<body>
    <h1>Facture {{invoiceNumber}}</h1>
    <p>Date: {{invoiceDate}}</p>
    <p>Client: {{customer.name}}</p>
    <p>Société: {{customer.company}}</p>
    <p>SIRET: {{customer.siret}}</p>
    <p>TVA: {{customer.tva}}</p>
    <p>Adresse: {{customer.address1}} {{customer.address2}}, {{customer.zipCity}}</p>
    <p>Email: {{customer.email}}</p>

    <table>
        <thead>
            <tr>
                <th>Produit</th>
                <th>Quantité</th>
                <th>Prix</th>
                <th>Total</th>
            </tr>
        </thead>
        <tbody>
            {{#each items}}
            <tr>
                <td>{{name}}</td>
                <td>{{quantity}}</td>
                <td>{{price}}€</td>
                <td>{{total}}€</td>
            </tr>
            {{/each}}
        </tbody>
    </table>

    <div class="totals">
        <p>Sous-total: {{totals.subtotal}}€</p>
        <p>Livraison: {{totals.shipping}}€</p>
        <p>Taxes: {{totals.tax}}€</p>
        <p><strong>Total: {{totals.total}}€</strong></p>
    </div>
</body>
</html>
